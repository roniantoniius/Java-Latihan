W Playlist of Java Spring Boot:
https://youtu.be/oGhc5Z-WJSw?si=8UbvTBNVlMCJDo05

Spring Certified Professional by Spring Academy, bayar kalau mau sertif $250:
https://spring.academy/paths

Good Udemy Spring Boot Microservices & Cloud (23 hr):
https://www.udemy.com/course/microservices-with-spring-boot-and-spring-cloud/

Ecommerce microservice spring boot (6 hr):
https://youtu.be/jdeSV0GRvwI?si=9AB_8tWKPVMEwFMo
- idk but theres no spring security, plus this guy using MongoRepository as JpaRepository crud functionality.

Spring Boot Microservices Job Platform, with efficient stack:
https://github.com/devxsb/spring-boot-microservices?tab=readme-ov-file#register
- Later gonna learn it, but spring boot foundation is necessery




		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>

====================================================================================


System Requirements
Spring Boot 3.4.3 requires at least Java 17 and is compatible with versions up to and including Java 23. Spring Framework 6.2.3 or above is also required.

Explicit build support is provided for the following build tools:

Build Tool	Version
Maven

3.6.3 or later

Gradle

Gradle 7.x (7.6.4 or later) or 8.x (8.4 or later)

Servlet Containers
Spring Boot supports the following embedded servlet containers:

Name	Servlet Version
Tomcat 10.1 (10.1.25 or later)

6.0

Jetty 12.0

6.0

Undertow 2.3

6.0

You can also deploy Spring Boot applications to any servlet 5.0+ compatible container.

GraalVM Native Images
Spring Boot applications can be converted into a Native Image using GraalVM 22.3 or above.

Images can be created using the native build tools Gradle/Maven plugins or native-image tool provided by GraalVM. You can also create native images using the native-image Paketo buildpack.

The following versions are supported:

Name	Version
GraalVM Community

22.3

Native Build Tools

0.10.5

------------------------------------------------------------------------------------

Using the @SpringBootApplication Annotation

Many Spring Boot developers like their apps to use auto-configuration, component scan and be able to define extra configuration on their "application class". A single @SpringBootApplication annotation can be used to enable those three features, that is:

- @EnableAutoConfiguration: enable Spring Boot’s auto-configuration mechanism
- @ComponentScan: enable @Component scan on the package where the application is located (see the best practices)
- @SpringBootConfiguration: enable registration of extra beans in the context or the import of additional configuration classes. An alternative to Spring’s standard @Configuration that aids configuration detection in your integration tests.


// Same as @SpringBootConfiguration @EnableAutoConfiguration @ComponentScan
@SpringBootApplication
public class MyApplication {

	public static void main(String[] args) {
		SpringApplication.run(MyApplication.class, args);
	}

}

@SpringBootApplication also provides aliases to customize the attributes of @EnableAutoConfiguration and @ComponentScan.


None of these features are mandatory and you may choose to replace this single annotation by any of the features that it enables. For instance, you may not want to use component scan or configuration properties scan in your application:

@SpringBootConfiguration(proxyBeanMethods = false)
@EnableAutoConfiguration
@Import({ SomeConfiguration.class, AnotherConfiguration.class })
public class MyApplication {

	public static void main(String[] args) {
		SpringApplication.run(MyApplication.class, args);
	}

}
In this example, MyApplication is just like any other Spring Boot application except that @Component-annotated classes and @ConfigurationProperties-annotated classes are not detected automatically and the user-defined beans are imported explicitly (see @Import).



-----------------------------------------------------------------------------------
Running Your Application
One of the biggest advantages of packaging your application as a jar and using an embedded HTTP server is that you can run your application as you would any other. The same applies to debugging Spring Boot applications. You do not need any special IDE plugins or extensions.

The options below are best suited for running an application locally for development. For production deployment, see Packaging Your Application for Production.
This section only covers jar-based packaging. If you choose to package your application as a war file, see your server and IDE documentation.
Running From an IDE
You can run a Spring Boot application from your IDE as a Java application. However, you first need to import your project. Import steps vary depending on your IDE and build system. Most IDEs can import Maven projects directly. For example, Eclipse users can select Import…​ → Existing Maven Projects from the File menu.

If you cannot directly import your project into your IDE, you may be able to generate IDE metadata by using a build plugin. Maven includes plugins for Eclipse and IDEA. Gradle offers plugins for various IDEs.

If you accidentally run a web application twice, you see a “Port already in use” error. Spring Tools users can use the Relaunch button rather than the Run button to ensure that any existing instance is closed.


Running as a Packaged Application
If you use the Spring Boot Maven or Gradle plugins to create an executable jar, you can run your application using java -jar, as shown in the following example:

$ java -jar target/myapplication-0.0.1-SNAPSHOT.jar
It is also possible to run a packaged application with remote debugging support enabled. Doing so lets you attach a debugger to your packaged application, as shown in the following example:

$ java -agentlib:jdwp=server=y,transport=dt_socket,address=8000,suspend=n \
       -jar target/myapplication-0.0.1-SNAPSHOT.jar


Using the Maven Plugin
The Spring Boot Maven plugin includes a run goal that can be used to quickly compile and run your application. Applications run in an exploded form, as they do in your IDE. The following example shows a typical Maven command to run a Spring Boot application:

$ mvn spring-boot:run
You might also want to use the MAVEN_OPTS operating system environment variable, as shown in the following example:

$ export MAVEN_OPTS=-Xmx1024m

Hot Swapping
Since Spring Boot applications are plain Java applications, JVM hot-swapping should work out of the box. JVM hot swapping is somewhat limited with the bytecode that it can replace. For a more complete solution, JRebel can be used.

The spring-boot-devtools module also includes support for quick application restarts. See the Hot Swapping section in “How-to Guides” for details.



====================================================================================
Dasar Spring/Boot:

Pengenalan Spring Framework
Spring Framework adalah framework paling populer di Java
Saking populernya, Spring Framework sampai mengalahkan popularitas Java Enterprise sendiri
Spring Framework dibuat sekitar tahun 2003 oleh Rod Johnson, yang dibuat sebagai alternative Java Enterprise
Spring Framework semakin populer karena sangat ringan dan mudah digunakan dibandingkan Java Enterprise
https://spring.io/ 

Pengenalan Spring Boot
Spring Boot merupakan framework untuk mempermudah pembuatan aplikasi Spring Framework
Dahulu untuk menggunakan Spring Framework, untuk pemula tidaklah mudah, karena terlalu banyak yang harus dilakukan sebelum bisa membuat aplikasi
Spring Boot menjadikan kompleksitas tersebut ditangani secara otomatis oleh Spring Boot, sehingga kita bisa membuat aplikasi Spring Framework secara cepat tanpa harus melakukan pengaturan apapun
Spring Boot sekarang sudah menjadi salah satu framework wajib ketika kita ingin membuat aplikasi Spring Framework

Jadi Spring merupakan core Framework atau blueprint inti, nah kemudian Spring Boot mengadopsi blue print itu.

Kenapa Menggunakan Spring?
● Tidak bisa dipungkiri, saat ini Spring adalah satu-satunya framework paling populer di Java
● Belum ada yang bisa menandingi popularitasnya di Java
● Saking populernya, bahkan banyak perusahaan pindah ke JVM karena ingin menggunakan
Spring-nya, bukan Java
● Dengan banyaknya bahasa yang bisa berjalan di atas JVM, seperti Kotlin, Groovy dan Scala, maka
secara programmer punya banyak pilihan bahasa pemrograman ketika menggunakan Spring
● Spring juga sudah banyak sekali diadopsi di banyak perusahaan, baik itu skala besar atau kecil

Ekosistem Pendukung
● Spring memiliki ekosistem pendukung yang sangat besar
● Spring sendiri tidaklah digunakan untuk menggantikan framework yang sudah ada, melainkan
menjahit framework-framework yang sudah ada, menjadi framework yang saling terintegrasi
● Spring bisa digunakan terintegrasi dengan baik dengan Bean Validation, Java Persistence API,
Servlet, dan lain-lain
● Selain itu juga Spring bisa diintegrasikan dengan teknologi yang tidak standar bawaan Java, seperti
MongoDB, Consul, Vault, Cassandra, dan lain-lain

Inversion of Control
● Inversion of Control (IoC) merupakan prinsip dalam pembuatan perangkat lunak, dimana kita
melakukan pemindahan kontrol untuk objek atau program ke sebuah container di framework
● Tidak seperti biasanya ketika kita membuat aplikasi, dimana kita selalu melakukannya secara
manual, dalam IoC, kita menyerahkan banyak pekerjakan ke container IoC
● Container IoC memiliki kontrol untuk melakukan eksekusi program kita, memanajemen object
pada program kita dan melakukan abstraction terhadap program kita
● Saat menggunakan framework IoC, kita biasanya akan mengikuti cara kerja framework tersebut

Spring Inversion of Control
● Spring bisa dibilang adalah framework IoC, dimana kita akan menyerahkan banyak sekali pekerjaan
dalam program kita ke Spring
● Kode program kita akan mengikuti cara kerja Spring

Application Context
● ApplicationContext adalah sebuah interface representasi container IoC di Spring
● ApplicationContext adalah inti dari Spring Framework
● ApplicationContext banyak sekali class implementasinya, secara garis besar dibagi menjadi 2 jenis
implementasi, XML dan Annotation
● Pada versi Spring 3, XML masih menjadi pilihan utama, namun sekarang sudah banyak orang
beralih dari XML ke Annotation, bahkan Spring Boot pun merekomendasikan menggunakan
Annotation untuk membuat aplikasi Spring
● https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/context
/ApplicationContext.html


Configuration
● Untuk membuat ApplicationContext menggunakan Annotation, pertama kita bisa perlu membuat
Configuration class
● Configuration Class adalah sebuah class yang terdapat annotation @Configuration pada class
tersebut

ApplicationContext context = new AnnotationConfigApplicationContext(BeanConfiguration.class);

Membuat Application Context
● Selanjutnya, setelah membuat Class Configuration, kita bisa menggunakan class
AnnotationConfigApplicationContext untuk membuat Application Context
● https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/context
/annotation/AnnotationConfigApplicationContext.html

Singleton
● Singleton adalah salah satu Design Patterns untuk pembuatan objek, dimana sebuah object hanya dibuat satu kali saja
● Dan ketika kita membutuhkan object tersebut, kita hanya akan menggunakan object yang sama
● https://refactoring.guru/design-patterns/singleton

Membuat Singleton di Java
● Ada banyak cara membuat singleton object di Java
● Cara yang paling sering digunakan adalah membuat class yang berisikan static method untuk
membuat object dirinya sendiri
● Selanjutnya constructor nya dibuat private, agar tidak bisa diakses dari luar
● Sehingga user terpaksa menggunakan method static tersebut ketika ingin membuat object nya

Bean
● Saat sebuah object kita masukkan kedalam Spring Container IoC, maka kita sebut object tersebut
adalah Bean
● Secara default, bean merupakan singleton, artinya jika kita mengakses bean yang sama, maka dia
akan mengembalikan object yang sama. Kita juga bisa mengubahnya jika tidak ingin singleton, nanti
akan kita bahas di materi tersendiri

Membuat Bean
● Untuk membuat bean, kita bisa membuat sebuah method di dalam class Configuration
● Selanjutnya nama method tersebut akan menjadi nama bean nya, dan return object nya menjadi
object bean nya
● Method tersebut perlu kita tambahkan annotation @Bean, untuk menandakan bahwa itu adalah
bean
● Secara otomatis Spring akan mengeksekusi method tersebut, dan return value nya akan dijadikan
object bean secara otomatis, dan disimpan di container IoC

Mengakses Bean
● Setelah kita membuat bean, secara otomatis semua object akan di-manage oleh Application
Context
● Untuk mengakses bean, kita bisa menggunakan method getBean milik Application Context

Duplicate Bean
● Di Spring, kita bisa mendaftarkan beberapa bean dengan tipe yang sama
● Namun perlu diperhatikan, jika kita membuat bean dengan tipe data yang sama, maka kita harus
menggunakan nama bean yang berbeda
● Selain itu, saat kita mengakses bean nya, kita wajib menyebutkan nama bean nya, karena jika tidak,
Spring akan bingung harus mengakses bean yang mana

Primary Bean
● Jika terjadi duplicate bean, selain kita sebutkan nama bean nya ketika ingin mengakses bean nya,
kita juga bisa pilih salah satu bean menjadi primary
● Dengan memilih salah satunya menjadi primary, secara otomatis jika kita mengakses bean tanpa
menyebutkan nama bean nya, secara otomatis primary nya yang akan dipilih
● Untuk memilih primary bean, kita bisa tambahkan annotaiton @Primary

Mengubah Nama Bean
● Secara default, nama bean diambil dari nama method
● Namun kadang-kadang kita tidak ingin menggunakan nama method untuk nama bean
● Saat project kita sudah besar, kadang bisa jadi nama method sama, walaupun isi bean nya berbeda,
dan di Spring, nama bean itu unik, tidak boleh sama
● Jika kita ingin mengubah nama bean, kita bisa menggunakan method value() milik annotation
@Bean

[54;56] mulai Dependency Injection jadi sejauh ini gunanya untuk otomatisasi membuat objek baru ketika kita memanggil objek itu sebagai constructor pada suatu objek. Ini katanya fitur dari Spring

Dependency Injection
● Saat kita membuat object, sudah pasti kita sering membuat object yang tergantung dengan object
lain
● Dependency Injection (DI) adalah teknik dimana kita bisa mengotomatisasi proses pembuatan object yang tergantung dengan object lain, atau kita sebut dependencies
● Dependencies akan secara otomatis di-inject (dimasukkan) kedalam object yang membutuhkannya
● Spring Framework sejak awal dibilang sebuah framework IoC yang memang cara kerjanya
menggunakan Dependency Injection

Tanpa Dependency Injection
● Sebenarnya tanpa Dependency Injection pun, kita tetap bisa membuat aplikasi
● Namun ketika relasi antar dependencies sangat kompleks, agak ribet untuk kita melakukannya jika
harus manual
● Oleh karena itu, penggunaan Dependency Injection framework seperti Spring sangat membantu
sekali



Spring Dependency Injection
● Spring sejak awal dikenal dengan framework untuk Dependency Injection
● Ketika kita membuat method untuk bean di Spring, kita bisa menambahkan parameter
● Secara otomatis Spring akan mencarikan bean lain yang sesuai dengan tipe parameter tersebut
● Jika ternyata tidak ada bean yang cocok, maka secara otomatis akan terjadi error
● Dan jika ternyata terdapat bean lebih dari satu, secara otomatis akan terjadi error, kecuali terdapat
primary bean


Memilih Dependency
● Kadang saat menggunakan DI, kita ingin memilih object mana yang ingin kita gunakan
● Saat terdapat duplicate bean dengan tipe data yang sama, secara otomatis Spring akan memilih
bean yang primary
● Namun kita juga bisa memilih secara manual jika memang kita inginkan
● Kita bisa menggunakan annotation @Qualifier(value=”namaBean”) pada parameter di method nya



Circular Dependencies
● Hati-hati dengan curcular dependencies
● Circular dependencies adalah kasus dimana sebuah lingkaran dependency terjadi, misal bean A
membutuhkan bean B, bean B membutuhkan bean C, dan ternyata bean C membutuhkan A
● Jika terjadi cyclic seperti ini, secara otomatis Spring bisa mendeteksinya, dan akan mengganggap
bahwa itu adalah error


Depends On
● Saat sebuah bean membutuhkan bean lain, secara otomatis bean tersebut akan dibuat setelah
bean yang dibutuhkan dibuat
● Namun bagaimana jika bean tersebut tidak membutuhkan bean lain, namun kita ingin sebuah bean
dibuat setelah bean lain dibuat?
● Jika ada kasus seperti itu, kita bisa menggunakan annotation @DependsOn(value={”namaBean”})
● Secara otomatis, Spring akan memprioritaskan pembuatan bean yang terdapat di DependsOn
terlebih dahulu


Lazy Bean
● Secara default, bean di Spring akan dibuat ketika aplikasi Spring pertama kali berjalan
● Oleh karena itu, kadang ketika aplikasi Spring pertama berjalan akan sedikit lambat, hal ini
dikarenakan semua bean akan dibuat di awal
● Namun jika kita mau, kita juga bisa membuat sebuah bean menjadi lazy (malas), dimana bean tidak
akan dibuat, sampai memang diakses atau dibutuhkan
● Untuk membuat sebuah bean menjadi lazy, kita bisa tambahkan annotation @Lazy pada bean
tersebut


Scope
● Scope merupakan strategy cara sebuah object dibuat
● Secara default strategy object di Spring adalah singleton, artinya hanya dibuat sekali, dan ketika
kita akses, akan mengembalikan object yang sama
● Namun kita juga bisa mengubah scope bean yang kita mau di Spring
● Untuk mengubah scope sebuah bean, kita bisa tambahkan annotation
@Scope(value=”namaScope”)


Bean Scope
1. singleton: (Default) Hanya dibuat sekali dalam Spring IoC
2. prototype: Selalu dibuat object baru setiap kali bean diakses
3. request: Dibuat baru per HTTP Request (hanya untuk Web App)
4. session: Dibuat baru per HTTP Session (hanya untuk Web App)
5. application: Dibuat baru per ServletContext (hanya untuk Web App)
6. websocket: Dibuat baru per WebSocket (hanya untuk WebSocket App)



Membuat Scope
● Jika scope yang disediakan oleh Spring tidak bisa memenuhi kebutuhan kita, kita juga bisa
membuat scope sendiri
● Caranya dengan membuat class yang implement interface Scope
● https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/beans/f
actory/config/Scope.html
● Selanjutnya untuk meregistrasikannya, kita bisa membuat bean CustomScopeConfigurer
● https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/beans/f
actory/config/CustomScopeConfigurer.html


Life Cycle
● Spring Container memiliki alur hidup, dan jika kita ingin berinteraksi dengan alur hidup nya Spring,
kita juga bisa lakukan
● Saat pertama kali Spring berjalan dan sudah selesai membuat bean, Spring akan memberitahu bean
tersebut bahwa bean tersebut sudah siap, artinya semua dependencies sudah dimasukkan.
● Dan ketika aplikasi Spring akan mati, Spring juga akan memberitahu semua bean bahwa bean
tersebut akan dihancurkan


Life Cycle Callback
● Secara default, bean tidak bisa tahu alur hidup Spring ketika selesai membuat bean dan ketika akan
menghancurkan bean
● Jika kita tertarik untuk bereaksi ketika alur hidup Spring terjadi, maka kita bisa implements
interface InitializingBean dan DisposableBean
● InitializingBean digunakan jika kita ingin bereaksi ketika Spring selesai membuat bean
● https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/beans/f
actory/InitializingBean.html
● DisposableBean digunakan jika kita ingin bereaksi ketika Spring akan menghancurkan bean
● https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/beans/f
actory/DisposableBean.html


Life Cycle Annotation
● Selain menggunakan interface InitializingBean dan DisposableBean, kita juga bisa menggunakan
annotation untuk mendaftarkan callback method untuk lifecycle
● Pada annotation @Bean, terdapat method initMethod() dan destoyMethod()
● initMethod() digunakan untuk meregistrasikan method yang akan dipanggil ketika bean selesai
dibuat
● destroyMethod() digunakan untuk meregistrasikan method yang akan dipanggil ketika bean akan
dihancurkan
● Methodnya harus tanpa parameter, dan return value nya tidak akan dipedulikan, jadi sebaiknya
gunakan void saja


@PostConstruct dan @PreDestroy
● Selain menggunakan annotation @Bean, untuk menandai sebuah method adalah init method dan
destroy method, kita juga bisa menggunakan annotation pada method nya secara langsung
● Ini bisa digunakan untuk menghindari kita lupa menyebutkan init dan destroy method ketika
membuat @Bean
● @PostConstruct merupakan method yang ditandai harus dipanggil ketika bean selesai dibuat
● @PreDestroy merupakan method yang ditandai harus dipanggil ketika bean akan dihancurkan
● Jika sudah menggunakan annotation ini, kita tidak perlu lagi menyebutkan nya di @Bean


Import
● Saat nanti kita membuat aplikasi, tidak mungkin kita hanya akan membuat satu Configuration
Class
● Biasanya kita akan membuat banyak sekali, tergantung seberapa kompleks aplikasi kita
● Spring mendukung import Configuration Class lain jika dibutuhkan
● Kita bisa menggunakan annotation @Import, lalu sebutkan Configuration Class mana yang ingin
kita import
● Ketika kita melakukan import, kita bisa import lebih dari satu class
● https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/context
/annotation/Import.html


[2:29:05]

Component Scan
● Import adalah hal yang sangat menarik, karena kita bisa memecah Configuration Class menjadi
banyak Class
● Namun semakin lama, pasti aplikasi kita akan semakin besar, dan secara otomatis Configuration
Class akan menjadi banyak
● Melakukan import satu persatu sudah tidak menyenangkan lagi jika terlalu banyak
● Spring memiliki fitur component scan, dimana kita bisa secara otomatis mengimport Configuration
di sebuah package dan sub package nya secara otomatis
● Untuk melakukan itu, kita bisa gunakan annotation @ComponentScan
● https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/context
/annotation/ComponentScan.html


Component
● Sebelumnya kita sudah belajar membuat bean menggunakan method di configuration dengan
annotation @Bean
● Spring juga menyediakan cara otomatis tanpa harus kita membuat method @Bean
● Kita bisa menandai sebuah class dengan annotation @Component, secara otomatis Spring akan
membuatkan bean untuk class tersebut
● @Component hanya mendukung pembuatan satu bean, jadi jika kita ingin membuat beberapa bean
dengan tipe yang sama, kita tetap harus menggunakan @Bean method

Component Bean Name
● Saat kita menggunakan @Component, nama bean akan secara otomatis dibuat otomatis oleh
Spring
● Jika kita menggunakan @ComponentScan, secara otomatis bean name secara otomatis akan
menggunakan nama class namun menjadi camelCase, misal CategoryService menjadi
categoryService, ProductWeb menjadi productWeb
● Jika kita menggunakan @Import, secara otomatis bean name akan menjadi
NamaClass.class.getName()
● Namun jika ingin membuat nama berbeda, kita juga bisa gunakan
@Component(value=”namaBean”)


Annotation
● Semua annotation yang sudah kita bahas di @Bean juga didukung di @Component
● Seperti misalnya @Scope, @PostConstruct, @PreDestroy, @Lazy, @Primary dan lain-lain




Constructor-based Dependency Injection


Dependency Injection
● Sebelumnya untuk melakukan Dependency Injection di @Bean, kita bisa menambah parameter di
method nya
● Secara otomatis Spring akan memilih bean mana yang cocok untuk parameter tersebut
● Bagaimana jika menggunakan @Component?
● Untuk @Component kita bisa melakukan beberapa cara untuk Dependency Injection nya



Constructor-based Dependency Injection
● Pertama yang bisa kita lakukan untuk Dependency Injection di @Component adalah menggunakan
constructor parameter
● Kita bisa menambahkan constructor yang memiliki parameter jika membutuhkan bean lain
● Secara otomatis Spring akan mencarikan bean tersebut, dan ketika membuat bean @Component,
Spring akan menggunakan bean yang dibutuhkan di constructor
● Constructor-based Dependency Injection hanya mendukung satu constructor, jadi pastikan kita
hanya membuat satu constructor


Multiple Constructor
● Seperti di awal disebutkan bahwa Spring hanya mendukung satu constructor untuk Dependency
Injection nya
● Namun bagaimana jika terdapat multiple constructor?
● Jika pada kasus seperti ini, kita harus menandai constructor mana yang akan digunakan oleh Spring
● Caranya kita bisa menggunakan annotation @Autowired




Setter-based Dependency Injection
● Selain menggunakan constructor parameter, kita juga bisa menggunakan setter method jika ingin
melakukan dependency injection
● Namun khusus untuk setter method, kita perlu menambah annotation @Autowired pada setter
method nya
● Secara otomatis Spring akan mencari bean yang dibutuhkan di setter method yang memiliki
annotation @Autowired
● Setter-based DI juga bisa digabung dengan Constructor-based DI




Field-based Dependency Injection
● Selain constructor dan setter, kita juga bisa melakukan dependency injection langsung
menggunakan field
● Caranya sama dengan setter, kita bisa tambahkan @Autowired pada fieldnya
● Secara otomatis Spring akan mencari bean dengan tipe data tersebut
● Field-based DI bisa digabung sekaligus dengan Setter-based DI dan Constructor-based DI
● Khusus Field-based DI, Spring sendiri sudah tidak merekomendasikan penggunaan cara melakukan
DI dengan Field



Qualifier
● Seperti yang sudah dijelaskan di awal, jika terdapat bean dengan tipe data yang sama lebih dari
satu, maka secara otomatis Spring akan bingung memilih bean yang mana yang akan digunakan
● Kita perlu memilih salah satu menjadi primary, yang secara otomatis akan dipilih oleh Spring
● Namun jika kita ingin memilih bean secara manual, kita juga bisa menggunakan @Qualifier
● Kita bisa tambahkan @Qualifier di constructor parameter, di setter method atau di field



Optional Dependency
● Secara default, semua dependency itu wajib
● Artinya jika Spring tidak bisa menemukan bean yang dibutuhkan pada saat DI, maka secara
otomatis akan terjadi error
● Namun jika kita memang ingin membuat sebuah dependency menjadi Optional, artinya tidak wajib
● Kita bisa wrap dependency tersebut dengan menggunakan java.util.Optional<T>
● Secara otomatis jika ternyata bean yang dibutuhkan tidak ada, maka tidak akan terjadi error
● Kita bisa gunakan Optional<T> pada @Bean (method parameter) ataupun @Component
(constructor parameter, setter method parameter, field)



Object Provider
● Selain menggunakan java.util.Optional<T>, kita juga bisa menggunakan ObjectProvider<T>
● Yang membedakan pada ObjectProvider<T> adalah, jika ternyata bean nya lebih dari satu,
ObjectProvider<T> bisa digunakan untuk mengambil semua bean tersebut
● https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/beans/f
actory/ObjectProvider.html


Factory Bean
● Kadang ada kasus dimana sebuah class misal bukanlah milik kita, misal class third party library
● Sehingga agak sulit jika kita harus menambahkan annotation pada class tersebut
● Pada kasus seperti ini, cara terbaik untuk membuat bean nya adalah dengan menggunakan @Bean
method
● Atau di Spring, kita juga bisa menggunakan @Component, namun kita perlu wrap dalam class
Factory Bean
● https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/beans/f
actory/FactoryBean.html



Inheritance
● Saat kita mengakses bean, kita bisa langsung menyebutkan tipe class bean tersebut, atau bisa juga
dengan parent class / parent interface bean
● Misal jika kita memiliki sebuah interface bernama MerchantService, lalu kita memiliki bean
dengan object implementasi class nya MerchantServiceImpl, maka untuk mengakses bean nya, kita
tidak hanya bisa menggunakan tipe MerchantServiceImpl, namun juga bisa dengan
MerchantService
● Namun perlu berhati-hati, jika misal MerchantService memiliki banyak bean turunan, pastikan
tidak terjadi error duplicate



Bean Factory
● ApplicationContext adalah interface turunan dari BeanFactory
● BeanFactory merupakan kontrak untuk management bean di Spring
● Method-method yang sebelumnya kita gunakan untuk mengambil bean, sebenarnya merupakan
method kontrak dari interface BeanFactory
● https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/beans/f
actory/BeanFactory.html


Listable Bean Factory
● Bean Factory hanya bisa digunakan untuk mengakses single bean, artinya jika terdapat bean
dengan tipe yang sama, kita harus sebutkan satu per satu nama bean nya
● Listable Bean Factory adalah turunan dari Bean Factory yang bisa kita gunakan untuk mengakses
beberapa bean sekaligus
● Dalam beberapa kasus, ini sangat berguna, seperti misal kita ingin mengambil semua bean dengan
tipe tertentu
● Application Context juga merupakan turunan dari interface Listable Bean Factory
● https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/beans/f
actory/ListableBeanFactory.html



Bean Post Processor
● Bean Post Processor merupakan sebuah interface yang bisa kita gunakan untuk memodifikasi
proses pembuatan bean di Application Context
● Bean Post Processor mirip seperti middleware, yang diakses sebelum bean di initialized dan
setelah bean di initialized
● Karena sangat flexible, bahkan Bean Post Processor bisa memodifikasi hasil object bean
● https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/beans/f
actory/config/BeanPostProcessor.html


Bean Id Generator
● Misal sekarang kita akan coba membuat id unique untuk bean
● Dimana kita akan membuat sebuah interface bernama IdAware, lalu memiliki method setId(String)
● Kita akan membuat Bean Post Processor, dimana jika bean nya implements IdAware, kita akan
setId(String) nya menggunakan UUID




Ordered
● Saat kita membuat Bean Post Processor, kita bisa membuat lebih dari satu
● Kadang ada kasus saat membuat beberapa Bean Post Processor, kita ingin membuat yang
berurutan
● Sayangnya secara default, Spring tidak menjamin urutan eksekusi nya
● Agar kita bisa menentukan urutannya, kita bisa menggunakan interface Ordered
● https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/core/Or
dered.html



Aware
● Spring memiliki sebuah interface berama Aware
● Aware adalah super interface yang digunakan untuk semua Aware interface
● Aware ini diperuntukkan untuk penanda agar Spring melakukan injection object yang kita
butuhkan
● Mirip seperti yang sudah kita lakukan ketika membuat IdAware menggunakan IdGenerator Bean
Post Processor
● Namun untuk ini, kita tidak perlu lagi membuat Bean Post Processor secara manual
● https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/beans/f
actory/Aware.html

Daftar Aware
1. ApplicationContextAware; Untuk mendapat application context
2. BeanFactoryAware:  Untuk mendapat bean factory
3. BeanNameAware : Untuk mendapat nama bean
4. ApplicationEventPublisherAware : Untuk mendapat event publisher
5. EnvironmentAware : Untuk mendapat environment



Bean Factory Post Processor
● Secara default, mungkin kita tidak akan pernah sama sekali membuat Application Context secara
manual
● Namun kadang ada keadaan kita ingin memodifikasi secara internal Application Context
● Spring merekomendasikan kita untuk membuat Bean Factory Post Processor
● https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/beans/f
actory/config/BeanFactoryPostProcessor.html
● https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/beans/f
actory/support/BeanDefinitionRegistryPostProcessor.html



Event Listener
● Spring memiliki fitur Event Listener yang bisa kita gunakan untuk komunikasi antar class
menggunakan Event
● Event di Spring merupakan object turunan dari ApplicationEvent, sedangkan Listener di Spring
merupakan turunan dari ApplicationListener
● https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/context
/ApplicationEvent.html
● https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/context
/ApplicationListener.html


Application Event Publisher
● Ketika kita ingin mengirimkan event ke listener, kita bisa menggunakan object
ApplicationEventPublisher, dimana ApplicationEventPublisher juga merupakan super interface
dari ApplicationContext
● Atau kita bisa menggunakan ApplicationEventPublisherAware untuk mendapatkan object
ApplicationEventPublisher
● https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/context
/ApplicationEventPublisher.html


Event Listener Annotation
● Selain menggunakan interface ApplicationListener, kita juga bisa menggunakan Annotation untuk
membuat Listener
● Ini lebih flexible dibanding menggunakan interface ApplicationListener
● Kita bisa menggunakan annotation @EventListener
● https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/context
/event/EventListener.html


Cara Kerja Event Listener Annotation?
● Annotation @EventListener bekerja dengan menggunakan Bean Post Processor bernama
EventListenerMethodProcessor
● EventListenerMethodProcessor mendeteksi jika ada bean yang memiliki method dengan
annotation @EventListener, maka secara otomatis akan membuat listener baru, lalu
meregistrasikannya ke ApplicationContext.addApplicationListener(listener)
● https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/context
/event/EventListenerMethodProcessor.html


Spring Boot Application
● Sebelumnya kita selalu membuat Application Context secara manual, dan kita belum menggunakan
fitur otomatis Spring Boot
● Hal ini sengaja saya jelaskan agar kita tidak kaget bagaimana semua itu bisa berjalan secara
otomatis di Spring Boot
● Pada saat membuat aplikasi Spring Boot, kita akan mengganti @Configuration utama menjadi
@SpringBootApplication
● Isi dari @SpringBootApplication itu sendiri sebenarnya gabungan dari @Configuration dan banyak
hal lain, seperti contohnya @ComponentScan
● https://docs.spring.io/spring-boot/docs/current/api/org/springframework/boot/autoconfigure/Sp
ringBootApplication.html

Spring Application
● Selain @SpringBootApplication, untuk membuat Application Context nya, kita tidak perlu
membuat manual, kita bisa gunakan class SpringApplication
● Secara otomatis SpringApplication akan membuat ApplicationContext dan melakukan hal-hal yang
dibutuhkan secara otomatis
● https://docs.spring.io/spring-boot/docs/current/api/org/springframework/boot/SpringApplicatio
n.html



Spring Boot Test
● Untuk membuat unit test di Spring Boot, kita bisa menggunakan annotation
@SpringBootTest(classes={YourConfiguration.class})
● Selanjutnya kita tidak perlu mengambil bean secara manual lagi menggunakan ApplicationContext,
kita bisa menggunakan DI secara langsung di unit test nya menggunakan @Autowired
● https://docs.spring.io/spring-boot/docs/current/api/org/springframework/boot/test/context/Spri
ngBootTest.html

Startup Failure
● Salah satu yang menarik di Spring Boot adalah, Spring Boot memiliki fitur FailureAnalyzer
● FailureAnalyzer digunakan untuk melakukan analisa ketika terjadi error startup yang
menyebabkan aplikasi tidak mau berjalan
● Spring sudah menyediakan banyak sekali class implementasi FailureAnalyzer yang bisa
mempermudah kita menganalisa kesalaha ketika terjadi startup failure
● https://docs.spring.io/spring-boot/docs/current/api/org/springframework/boot/diagnostics/Failu
reAnalyzer.html


Banner
● Spring Boot memiliki fitur banner, dimana saat aplikasi Spring Boot berjalan, kita bisa menampilkan
tulisan banner di console
● Secara default fitur banner ini akan menyala dan akan mencari tulisan banner di classpath dengan
nama banner.txt
● Jika tidak ada file tersebut, maka secara otomatis akan menampilkan tulisan banner Spring Boot
● Salah satu contoh tempat untuk membuat banner adalah http://www.bagill.com/ascii-sig.php



Customizing Spring Application
● Kadang ada kalanya kita ingin melakukan pengaturan di Spring Application sebelum Application
Context nya dibuat
● https://docs.spring.io/spring-boot/docs/current/api/org/springframework/boot/SpringApplicatio
n.html
● Kita bisa menggunakan langsung SpringApplication, atau bisa juga menggunakan
SpringApplicationBuilder
● https://docs.spring.io/spring-boot/docs/current/api/org/springframework/boot/builder/SpringAp
plicationBuilder.html



Spring Application Event
● Sebelumnya kita sudah belajar tentang Event Listener
● Di Spring Boot, terdapat banyak sekali Event yang dikirim ketika aplikasi Spring Boot berjalan
● Jika kita ingin, kita bisa membuat Listener untuk menerima event tersebut
● https://docs.spring.io/spring-boot/docs/current/api/org/springframework/boot/context/event/Sp
ringApplicationEvent.html


Daftar Event
1. ApplicationStartingEvent : Dikirim ketika start aplikasi
2. ApplicationContextInitializedEvent : Dikirim ketika ApplicationContext sudah di initialisasi
3. ApplicationStartedEvent : Dikirim ketika aplikasi sudah berjalan
4. ApplicationFailedEvent : Dikirim ketika aplikasi gagal berjalan


Menambah Listener
● Beberapa Event di Spring Boot Application Event di trigger bahkan sebelum Spring membuat
Application Context
● Oleh karena itu, jika kita buat menggunakan bean, bisa saja beberapa listener tidak akan dipanggil,
karena bean nya belum dibuat
● Agar lebih aman, kita bisa menambahkan listener ketika membuat SpringApplication 


Command Line Runner
● Saat kita membuat aplikasi, kadang kita butuh argument yang diberikan pada main method
● Spring Application bisa mengirim data argument tersebut secara otomatis ke bean yang kita buat
● Kita hanya butuh membuat bean dari CommandLineRunner
● CommandLineRunner secara otomatis akan di jalankan ketika Spring Application berjalan
● https://docs.spring.io/spring-boot/docs/current/api/org/springframework/boot/CommandLineRu
nner.html


Application Runner
● Selain CommandLineRunner, Spring Boot menyediakan fitur ApplicationRunner
● Penggunaan ApplicationRunner sama seperti CommandLineRunnnner, hanya saja argument nya
sudah di wrap dalam object ApplicationArguments
● Yang menarik dari ApplicationArguments adalah, memiliki fitur parsing untuk command line
argument
● https://docs.spring.io/spring-boot/docs/current/api/org/springframework/boot/ApplicationArgu
ments.html
● https://docs.spring.io/spring-boot/docs/current/api/org/springframework/boot/ApplicationRunn
er.html


Spring Boot Plugin
● Saat kita membuat project Spring Boot, secara otomatis terdapat spring-boot-plugin di project
maven kita
● Plugin ini bisa digunakan untuk mempermudah saat kita menjalankan aplikasi Spring kita
● Kita bisa gunakan perintah :
mvn spring-boot:run
● Untuk menjalankan aplikasi Spring Boot kita, kita harus memastikan bahwa hanya ada satu main
class



Distribution File
● Spring Boot plugin juga sudah menyediakan cara membuat distribution file aplikasi kita
● Plugin ini akan mendeteksi main class di project kita, lalu membundle aplikasi kita beserta
dependency yang dibutuhkan dalam satu file jar
● Pastikan hanya terdapat satu main class, karena jika lebih dari satu, maka spring boot plugin akan
melakukan komplen
● Kita cukup gunakan perintah :
mvn package
● Secara otomatis akan terbuat single jar application



===================================================================================

[Setting / Pertama, siap-siap]
Apache Maven:
Singkatnya apaache maven digunakan untuk memanage project dan semua yang berkaitan untuk membuild programs.

./mvnw clean compile

format perintah maven CLI
mvnw [options] [<goal(s)>] [<phase(s)>]


Apache Maven memliki beberapa fase dalam suatu siklus yang menjadi langkah spesifik untuk mencapai tujuan. Mulai dari fase `clean`, `default`, dan `site`.
- clean: menghapus files sementara yang tujuannya tidak terlalu signifikan
- default: tahap dimana sebagian besar tugas untuk mencapai tujuan seperti build dan testing
- site: semua dokumentasi digenerate


Tahap `clean`: mvnw clean
- pre-clean: hook sebelum cleaning
- clean: melakukan cleaning
- post-clean: hook setelah cleaning


Tahap `default`: mvnw [default]
- compile: compiles java code into bytecode
- test: menjalankan unit test
- package: membuat project menjadi jar atau war file
- verify: melakukan pemeriksaan runs dan integrasi test

Maven workflow:
biasanya compile dulu, kemudian clean test, lalu clean package, and then run the app with:
`java -jar [nama jar]` kalau pakai kustom server = `java -jar [nama jar] --server.port=8081`


cara run app

.\mvnw spring-boot:run

Jadi untuk buat rest API kita menggunakan Spring Framework yang sebagian besar mendukung proses rest api. Nah kenapa nggak Spring Boot? Karena Spring Boot sendiri dibangung di atas layer dari Spring Framework. Penggunaan Spring Framework sangat mungkin di configure namun memang butuh banyak effor.



3.2. Spring App Layers
Spring boot adalah layer yang berdiri di atas Spring framework. Nah kalau dilihat dari Spring framework ternyata berisikan beberapa Layer yang tujuannya untuk menyelesaikan masalah. Jadi anggap aja aplikasi kamu merupakan suatu layer itu sendiri.

Layer dalam Spring Framework:

1. Persistance Layer
Lapisan yang akan mengatasi interaksi dengan database (H2, Postgre, dll). Biasanya menggunakan entities pada lapisan ini untuk berinteraksi dengan sebuah database menggunakan beberapa pola:
a. Pola Entities


b. Pola Repositories / Data Access Objects (DAOs)


Persistance layer biasanya digunakan untuk proses CRUD (Create, Read, Update, Delete). Tujuan dari layer ini untuk berinteraksi terhadap berbagai layer yang membutuhkan akses ke database, atau menampilkan data melalui interfaces


2. Service Layer
Lapisan yang bertujuan untuk menggunakan semua functionality yang ditawarkan oleh Persistance layer dan menggunakannya untuk bagaimana aplikasi dibangun. Biasanya diterapkan dengan beberapa kumpulan interfaces dan mengimplementasikannya dengan clases tertentu atau Services. Kita harus menerapkan fungsionalitas Persistance layer langsung terhadap Service layer, jangan sampai komunikasi melalui Persistance ke layer Presentation secara langsung.


3. Presentation Layer
Lapisan yang bertujuan untuk mengambil semua data yang didapat melalui Service layer dan menampilkannya ke User, biasanya melalui rest API dengan menggunakan konsep Controller atau melalui graphqlAPI atau websocketAPI.


3.3. Modularity


Penerapan dari setiap layer tersebut kita bisa mulai dari Persistance Layer:
- Include depencencies di pom xml
- Masukkan konfigurasi di properties file mengenai koneksi ke database
- Semua bean yang berkaitan akan dibuat secara otomatis
- setelah itu fokus ke logic 



4.1. Inversion of Control atau Dependency Injection

udah ada penjelasan di atas



4.2. Beans
Menurut aku sendiri Bean ini semacam suatu anotasi yang menandakan method di bawahnya mampu dikenali oleh Spring secara otomatis. Biasanya Beans digunakan untuk menerapkan Dependency Injection, Spring akan melihat Bean dengan tipe data mana yang secara default dipilih, biasanya pakai Primary atau dinamain. Ketika suatu constructor yang menerima method dengan tipe yang sama dengan method Beans sebelumnya, maka Bean akan melakukan Dependency Injection menggunakn method itu.

Tapi file yang mengandung Beans seharusnya menerapkan anotasi Configuration, supaya Spring dapat mengenalinya


4.3. Components
Components juga digunakan untuk menandakan suatu Class bahwa method yang dimiliki merupakan suatu Bean. Jadi bisa dibilang Bean digunakan untuk suatu method namun file tersebut harus memiliki anotasi Configuration. Sedangkan penggunaan Components pada awal class lebih simpel.

4.4. Component Scanning
Merupakan kemampuan Spring dalam mendeteksi suatu Bean dan melihat dimana Bean akan diterapkan secara otomatis ketika aplikasi dijalankan.

Anotasi @ComponentScan itu sudah terkandung dalam anotasi @SpringBootApplication



4.5. @SpringBootApplication

memiliki beberapa hal mulai dari
@SpringBootConfiguration
@ComponantConfiguration
@ComponentScan
@EnableAutoConfiguration


4.6. @AutoConfiguration
Jadi anotaassi ini akan bekerja ketika Spring Boot berjalan yang akan memulai semua hal default dan membuat semua dependencies

SpringBootStart merupakan Collection dari dependencies yang diurut untuk menyelesaikan permasalhaan.



5.2. Environment Variables
Jadi kita bisa setting kalau di Eclipse melaui menu Run > Run Configuration > Tab Environments.

Atau bisa juga dari Command prompt, terus nanti di EXPORT, baru habis itu bisa kita Jar



6.1. Database Layers:
Untuk penerapan database layer itu ada beberapa:

1. Database driver
Database umum yang didukung oleh Spring seperti mysql atau postgre yang mampu menyediakan db khusus untuk java.

2. JDBC (Java Database Connectivity)
Merupakan level Load Data API yang bisa berinteraksi dengan database berdasarkan kueri SQL. Tapi kita harus Mapping dari dan menuju java object itu sendiri

3. JPA (Java Persistance API)
Jadi disini kita berinteraksi terhadap database menggunakan Java Objects (sama kayak ORM di Flask mungkin?, ORM adalah proses mapping dari SQL ke Java Objects dan kembali lagi). Tapi biasanya Spring menggunakan ini dengan istilah Hibernate.


6.2. Connect to a H2 Database
Jadi di spring initializr kita bisa langsung pilih H2 extension, lalu pakai objek DataSource di class utama. Magicnya adalah secara default database H2 udah otomatis terkoneksi. Jadi karena Auto Configures dalam Spring Boot sehingga kalau menemukan H2 di class path maka otomatis membuat Bean untuk koneksi.

Kalau di .properties kita juga bisa setting:
spring.application.name=database
spring.datasource.url=jdbc:h2:mem:testdb
spring.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password

h2 adalah tipe database, 'mem' in memory database, 'testdb' merupakan nama database
Kita koneksi menggunakan driverClassName org.h2.Driver.


6.3. Connect to PostgreSQL
Kurang lebih sama, namun ketika initialize jangan lupa tambah dependencies Postgre, JDBC API, dan lombok. Nah untuk setting postgre kita bisa pakai docker compose lalu jangan lupa juga di properties:

docker compose.yml:
services:
  db:
    image: postgres
    ports:
      - "5432:5432"
    restart: always
    environment:
      POSTGRES_PASSWORD: passkord


properties:
spring.application.name=databasepostgre
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=passkord


6.4. Initialize DB Schema


7.1. JDbcTemplate Setup
Disini kita bikin setup Configuration mengandung metode Bean dengan tipe JdbcTemplate untuk melakukan kueri. Makannya kita butuh DAO

7.2. What is DAO (Data Access Object)

DAO merupakan sebuah interface yang diterapkan pada suatu layer service untuk memahami bagaimana data diambil dari database melalui persistance layer, data tersebut didapat dari dan menuju kueri menggunakan SQL dengan java object. Caranya DAO akan mapping semacam bean ke persistance layer.


7.3. Set Up DAOs
Kurang lebih cara setupnya dengan menerapkan kerangka si Data nya melalui folder domain, and then set up interface dan implementasinya


7.4. Create DAOs
Disini kita menerapkan C pada CRUD. Caranya dengan untuk setiap interface kita bikin method Create yang menerima parameter tipe Data java object. Lalu dari fungsi implementasinya kita pakai jdbc template untuk create data dengan method update lalu kueri INSERT INTO, pastikan disini kita bikin opsi input (?, ?) untuk handle SQL Injection.

7.5. Implement Read DAOs
Jadi ada dua tip Read, yang pertama adalah findOne() untuk mencari satu data berdasarkan id serta findMany() untuk mencari beberapa data yang biasanya disimpan dalam List

Kurang lebih penerapannya sama ketika Create, namun pada test kita menggunakan verify terhadap .query() dengan menerapakan Row Mapper dalam mengatasi konversi dari SQL dan menuju Java Objects

7.6. Integration Test
Disini kita bakal test dengan menjalankan sql query pada H2 Database in memory sql postgre


7.7. Find Many
kurang lebih sama, cuman ditampung dalam list, dan karena ada beberapa test case, sehingga ketika ada data yang sama. Kita harus clean dulu database setiap test yang berbeda dengan anotasi @DirtiesContext(classMode = ClassMode.AFTER_EACH_TEST_METHOD)

7.8. Full Update
Jadi caranya sama aja sih, intinya kuerian, tapi untuk Update kita bikin method di interface yang menerima constructor id dan objek


7.9. Delete
Caranya juga sama method dengan parameter id dengan query DELETE bla bla blas


8.1. Spring Data JPA Setup
Jadi katanya ada cara yang lebih mudah dalam melakukan CRUD menggunakan JPA dan Hibernate

8.2. Create Entities
Untuk memulai CRUD dengan JPA kita harus bikin objek domain dengan entity. Pada class object dengan anotasi @Data kita tandai dengan anotasi @Entity yang menandakan bahwa kelas ini sebagai entitas yang akan digunakan oleh JPA.

Jangan lupa tandain class itu dengan nama tabel yang benar melalui anotasi @Table(name = "namatabel"). Lalu untuk initiate variabel khususnyna id kita tandain dengan anotais @Id dan jika ingin otomatis nilainya (bertambah) maka tambah anotasi @GeneratedValue(strategy = GenerationType.SEQUENCE, generator = "namatabel_id_seq")

*seq stands for sequence, and those Entity and Tabel anotation come from Jakarta package

Kalau pada suatu tabel memiliki foreign key, maka variabel tersebut kita anotasi dengan relasinya seperti @ManyToOne(cascade = CascadeType.ALL) (artinya dari tabel tersebut Many ke tabel tujuan One), lalu dibawahnya kita sambungin dengan anotasi @JoinColumn(name = "namatabel_id")


Jadi kan emang agak bingung kok disini kita hubungin variabel atau nama sequencenya tidak serupa dengan nama variabel. Nah disinilah peran dari Hibernate Auto DDL

8.3. Hibernate Auto DDL
Berguna untuk mensetup database schema dan caranya cukup straightforward. Caranya di properties tambahin `spring.jpa.hibernate.ddl-auto=update` biar hibernate bisa bikin schema 

8.4. Create & Read One Using JPA and Hibernate

Create
Untuk fungsionalitas ini kita harus meng Extends class repository dengan CrudRepository<objekKelas, tipedataId>. Lalu menggunakan anotasi @Repository yang bertujuan serupa dengan @Component namun menandkan bahwa class ini merupakan repository. Artinya membuat Bean yang bisa diinject dimanapun

Untuk Create khususnya pada test sebenarnya tidak terlalu perlu karena JPA suddah otomatis. Namun ketika ingin menambah suatu java objek ke suatu tabel di database kita cuman menggunakan method .save(Objek objek). Method ini diambil dari class sebelumnya melalui CrudRepository

8.5. Implement Find All

Sama tapi kita pakai method .findAll dan menyimpannya dalam variabel dengan tipe data Iterable<namaklas>

8.6. Update
Unntuk ini sama aja, jadi penggunaan method .save yang awalnya untuk membuat baru, itu juga bisa untuk update

8.7. Delete
Menggunakan method .deleteById(input.id()) atau method .delete(Objek)

8.8. Custom Queries
Kita bisa membuat kueri hanya dengan menamakan sbuah method di dalam repo


8.9. HQL
HQL stands for Hibernate Query Language, a query language that is used to retrieve and manipulate data from a database. It is similar to SQL (Structured Query Language), but is more object-oriented. 
How does HQL work?
HQL is used with Hibernate, an object-relational mapping framework in Java. 
HQL allows developers to write portable queries that work across different databases. 
HQL can be used to perform database operations such as selecting, inserting, updating, and deleting data. 
HQL is a superset of JPQL, which is a subset of HQL

Jadi pada interface dari method yang menerapkan CrudRepository<namakelas, long> di atas method tersebut kita terapkan HQL seperti ini:
@Query("SELECT a FROM Ppn WHERE a.ppnYear > ?1") // artinya mengambil input dari parameter pertama



---------------------------------------------------------------------------------
Belajar RestAPI

9.1. What is Jackson?
Jackson merupakan library/package pada Spring Framework untuk berinteraksi dengan Json. Jackkson ada untuk mengambil Java Object dan mengubahnya menjadi Json melalui proses yang disebut dengan Marshalling dan melakukan sebaliknya, mengambil Json dan mengubah menjadi Java Object dengan proses Unmarshalling.

9.2. Jackson & Spring Web, membuat API sederhana
Caranya dengan membuat class dengan anotasi @Data dan @Builer lalu menginisiasikan setiap variabel. Setelah itu pada package Controllers kita bikin class menerapkan anotasi @RestController lalu membuat dua method Get dan Post melalui anotasi @GetMapping dan @PostMapping dengan path tertentu.

Nah nanti pada method dengan anotasi @GetMapping kita bikin .builder() sebagai default data. Sedangkan untuk methode @PostMapping menerima parameter Java Objek dengan anotasi @RequestBody

Setelah itu kita coba API nya di PostMan.

Jadi intinya ketika dicoba di Postman, untuk GET maka bakal menerima Java Objek lalu mengconvertnya menjadi JSON.


9.3. Java Objects to JSON
Disini kita akan menggunakan Jackson Object Mapper, lalu untuk mengubah dari Java Object ke JSON Object kita bisa menginisasi object ObjectMapper lalu memanggilnya menggunakan method .writeValueasString(Object objek) dan simpan di variabel String.

Sebaliknya kita perlu memanggil method .readValue(String hasilApi, Object.class)

9.4. Renaming JSON properties
Cara untuk handle ketika nama variabel di JSON berbeda dengan nama variabel di Jvaa Object adalah dengan menambah anotasi di atas variabel yang namanya berbeda tersebut dengan @JsonProperty("nama")


9.6. JSON Ignore
Jika ada suatu variabel pada JSon yang tidak ingin kita terima dan tidak didefinisikan pada class @Data, maka kita cukup tambahkan anotasi @JsonIgnoreProperties(ignoreUnknown = true) di awaal class



10.1. RestAPI Design
Jadi konsep fundamental dalam RestAPI itu dibangun dalam Resources, jadi semua struktur url itu tentang Resources.

Jadi fun fact, kalau kita pakai POST itu biasanya ada variabel id yang di mana tergenerate secara otomatis. Sedangkan jika id merupakan hasil input, maka kita menggunakan PUT.

Fun fact lagi kalau kita pakai PATCH itu fungsinya sama seperti PUT yaitu update atau bikin yang baru, namun PUT itu harus dilakukan secara keseluruhan, jadi semua variabel harus ada isinya. Namun untuk PATCH itu kita bisa menggunakaannya untuk mengubah secara parsial. Misalnya hanya ingin edit bagian 'nama'.


Intinya tugas kita kali ini bikin beberapa endpoint untuuk tabel kapal dan ppn

Endpoint Kapal:
POST	/kapal-kapal
GET	/kapal-kapal/{idkapal}
GET	/kapal-kapal		// findAll() disimpan di Iterable<Object>
PUT	/kapal-kapal/{idkapal}	// edit
PATCH	/kapal-kapal/{idkapal}	// edit suatu variabel
DELETE	/kapal-kapal/{idkapal}

Endpoint Ppn
POST	/ppn
GET	/ppn/{id}
GET	/ppn
PUT	/ppn/{id}
PATCH	/ppn/{id}
DELETE	/ppn/{id}

10.2. Ppn Create Endpoint

Jadi kalau ada folder Controller itu merupakan layer Presentation

[4:01:49] mulai bikin endpoint Ppn tapi lagi tambahiin layer Service ke Controller

Jadi gini pada layer Presentation (folder Controller) ada yang namanya data transfer object. Merupakan sebuah objek yang membawa data dalam prosesnya. Jadi kita harus memisahkan data atau business logic yang ada pada Service layer (folder services atau interface suatu Objek) supaya tidak ditampilkan pada layer Presentation. Jadi DTO akan mapping data dari service layer, kemudian hasil dari DTO lah yang dikirim ke Presentation layer (Request Body, dll).

Jadi caranya kita pakai module modelmapper yang bisa convert si DTO menjadi entities (ini yang kita gunakna di business logic)

Jadi rutenya itu untuk menampilkan data dari Service Layer (yang berinteraksi dengan Persistance layer) menuju Presentation Layer. Kita harus mapp si entity yang ada di service dengan menggunakan ModelMapper menuju ke objek DTO.


So to summarize each file purpose:
Based on the Spring Boot log and the typical Spring application structure, here's a description of the key files and their roles in the Spring lifecycle, from persistence to presentation layer:

## Persistence Layer

1. **Entity Classes (e.g., PpnEntity.java, KapalEntity.java)**
   - Define the data model
   - Map Java objects to database tables using JPA annotations
   - Represent the structure of database tables in Java code

2. **Repository Interfaces (e.g., PpnRepository.java)**
   - Define database operations
   - Extend Spring Data interfaces (e.g., CrudRepository)
   - Provide method declarations for custom queries

## Service Layer

3. **Service Interfaces (e.g., PpnService.java)**
   - Define business logic operations
   - Act as an abstraction layer between controllers and repositories

4. **Service Implementations (e.g., PpnServiceImpl.java)**
   - Implement service interfaces
   - Contain the actual business logic, kayak Create, Find ALl, dll
   - Interact with repositories to perform data operations

## Presentation Layer

5. **Controllers (e.g., PpnController.java)**
   - Handle HTTP requests
   - Map URLs to specific methods
   - Use services to process requests and prepare responses
   - Return data or views to the client

## Configuration

6. **Application Properties (application.properties or application.yml)**
   - Configure database connection details
   - Set various Spring Boot and application-specific properties

7. **Main Application Class (DatabasepostgreApplication.java)**
   - Entry point of the Spring Boot application
   - Contains the main method to run the application
   - Often includes additional configuration through annotations

## Data Transfer

8. **DTOs (Data Transfer Objects) (e.g., PpnDto.java)**
   - Used to transfer data between layers
   - Often used to shape the data sent to and received from the presentation layer

## Mapping

9. **Mappers (e.g., Mapper.java mentioned in PpnController)**
   - Convert between entity objects and DTOs
   - Help in separating database representation from API representation

This structure follows the typical flow in a Spring application:
1. HTTP request received by a Controller
2. Controller calls appropriate Service method
3. Service performs business logic, often using Repositories
4. Repositories interact with the database through Entity classes
5. Data flows back up the chain, often converted to DTOs before being sent to the client


# Penjelasan Kode dalam Bahasa Indonesia

Berikut penjelasan detail tentang kode yang diberikan dalam konteks Spring lifecycle dari Persistence Layer hingga Presentation Layer:

## 1. Tujuan Keseluruhan Kode

Kode ini merupakan implementasi aplikasi Spring Boot untuk mengelola data kapal dan PPN (Pelabuhan Perikanan Nusantara). Aplikasi ini memiliki kemampuan untuk:
- Menyimpan data kapal beserta informasi PPN yang terkait dengannya
- Mengambil dan menampilkan data kapal
- Mengimplementasikan relasi Many-to-One antara kapal dan PPN

## 2. Arsitektur dan Lapisan (Layers)

Aplikasi ini menggunakan arsitektur berlapis (layered architecture) yang umum dalam Spring:

### a. Persistence Layer (Lapisan Persistensi)
- **Entity Classes**: `KapalEntity` dan `PpnEntity` yang merepresentasikan tabel dalam database
- **Repository Interfaces**: `KapalRepository` dan `PpnRepository` untuk operasi CRUD namunn biasanya digunakan untuk custome Query

### b. Service Layer (Lapisan Layanan)
- **Service Interfaces**: `KapalService` dan `PpnService` merupakan sebuah interfaces yang mendefinisikan operasi bisnis
- **Service Implementations**: `KapalServiceImpl` dan `PpnServiceImpl` yang mengimplementasikan operasi bisnis. Disini berisikan method untuk proses CRUD dan cara penggunaanya melaui Dependency Injection dengan Beans yang dipanggil.

### c. Presentation Layer (Lapisan Presentasi)
- **DTO Classes**: `KapalDto` untuk transfer data antara lapisan
- **Mappers**: `KapalMapperImpl` untuk transformasi antara entity dan DTO
- **Controllers**: `KapalController` untuk menangani HTTP requests dan responses

## 3. Alur Kerja (Workflow) dalam Spring Lifecycle

Berikut alur kerja aplikasi ketika menghandle request untuk membuat kapal baru:

### a. Request Masuk ke Controller
1. Request HTTP POST diterima oleh `KapalController` pada endpoint `/kapal-kapal`
2. Controller menerima data JSON yang dikonversi menjadi objek `KapalDto`

### b. Transformasi Data (DTO ke Entity)
1. Controller memanggil `kapalMapper.mapFrom(kapal)` untuk mengubah `KapalDto` menjadi `KapalEntity`
2. Dalam proses mapping, `KapalMapperImpl` mencari PPN berdasarkan ID dengan memanggil `ppnRepository.findById()`
3. PPN yang ditemukan diset ke `KapalEntity` sebagai `ppnid`

### c. Proses Bisnis di Service Layer
1. Controller memanggil `kapalService.createKapal(kapalEntity)` untuk memproses data
2. `KapalServiceImpl` menerima `KapalEntity` dan menjalankan operasi bisnis jika diperlukan
3. Kemudian memanggil `kapalRepository.save(kapalEntity)` untuk menyimpan data

### d. Persistence Layer Bekerja
1. `KapalRepository` (extend dari `CrudRepository`) menangani operasi penyimpanan data
2. Spring JPA mengkonversi entity menjadi SQL dan menjalankannya pada database
3. Database PostgreSQL menyimpan data dan mengembalikan entity yang tersimpan dengan ID baru

### e. Response Kembali ke Client
1. Entity hasil disimpan kembali ke Service Layer
2. Service Layer mengembalikan entity ke Controller
3. Controller memanggil `kapalMapper.mapTo(hasilKapal)` untuk mengubah entity kembali menjadi DTO
4. Controller mengembalikan `ResponseEntity` dengan status HTTP 201 (Created) dan DTO kapal yang baru dibuat

## 4. Interaksi dengan Database

- Aplikasi menggunakan Spring Data JPA untuk manipulasi database
- Secara otomatis ada transaksi yang dikelola oleh Spring
- Menggunakan strategi `GenerationType.SEQUENCE` untuk pembangkitan ID
- Menggunakan `@ManyToOne` untuk relasi antara kapal dan PPN

## 5. Penjelasan Perbaikan Kode Test

Pada kode test yang diperbaiki:

1. **Masalah asli**: Test mengirimkan `KapalEntity` langsung, padahal controller menerima `KapalDto`
2. **Perbaikan**: 
   - Membuat dan menyimpan `PpnEntity` terlebih dahulu sehingga memiliki ID valid
   - Membuat `KapalDto` dengan ID PPN yang sudah disimpan
   - Mengirim DTO ini ke controller, bukan entity
3. **Alur kerja test yang benar**:
   - Buat dan simpan PPN terlebih dahulu
   - Buat KapalDto dengan ID PPN tersebut
   - Kirim DTO ini ke endpoint controller
   - Validasi response dengan JsonPath

Perbaikan ini memastikan bahwa test mencerminkan alur kerja nyata aplikasi dan menggunakan tipe data yang tepat di setiap lapisan.


10.5. Membuat Ppn List Endpoint
Kita bikin endpoint dan method di implementasi dari Service untuk findAll lalu dipangil di Controller, dan untuk logikanya ada di MapperConfig


10.7 Mengambill satu Ppn dari Controller
Caranya cukup simpel tinggal tambah controller path dengan tipe Get 'path/{id}' pada presentation layer. Lalu tambahkan method untuk findById (bisa diakses pada PersentationLayer) pada interface servis

10.9. Ppn Full Update
Kalau full update caranya di Controller (@PutMapping) kita set si id dari requestbody Dto adalah sesuai input dari endpoint api 'api/v1/{1}', setelah itu dari objek Dto kita ubah ke Entity dan save entity ke database menggunakan method .save (sebelumnya createPpn, karena fungsionalitasnya sama jadi disamain aja). Lalu dikirim kembali dalam bentuk Dto dengan conver menggunakan mapTo dari Mapper.

10.11 Ppn Partial Update
Nah disini pada kontroller @PatchMapping "/ppn/{id}". Kita masih sama pada parameter membutuhkan id dan objek Dto. Lalu kita nggak perlu set Id pada requesstbody objek Dto. Kita langsung ubah dulu menjadi entitas supaya bisa diolah di database (service), nah kita menggunakan method .partialUpdate(Long id, PpnEntity ppn) yang menerima id dan entitas. Intinya method ini akan menggunakna entitas dengan id tertentu, lalu mengedit suatu variabel dengan cara .map() terhadap ppnRepository (objek Crud) dengan mencari entitas menggunakan findById. Lalu dalam fungsi .map tersebut kita melakukan edit terhadap suatu variabel. Caranya menggunakan `Optional.ofNullable(ppnEntity.getName()).ifPresent(entityAda::setName)` artinya akan menjadi opsional (null atau berisi) untuk edit suatu variabel kalau variabel tersebut ada.

[5:31:40]


10.13. Ppn Delete
Jadi caranya sama tapi di controller kita pakai anotasi @DeleteMapping, kita cukup panggil method .delete() yang bakal didefinisiin di layer Service menggunakan method .deleteById() dari method bawaah Crud Repository.


10.15 Nested Objects
Jadi ketika pada Dto dia menerima beberapa variabel dan salah satunya ada sebuah Entity, nah kita kan pengen simpel atau biar sekalian aja dibuat dalam satu response body membuat Kapal lalu membuat Ppn juga secar otomatis. Caranya menggunakan model mapper.

ModelMapper modelMapper = new ModelMapper();
modelMapper.getConfiguration().setMatchingStrategy(MatchingStrategies.LOOSE);
return modelMapper;

Jadi dengan menerapkan getConfiguration() dengan method setMatchingStrategy() dengan parameter MatchingStrategies LOOSE kita memberi tahu Spring Data JPA bahwa kita akan mengirim nested Objek dalam Dto response body. Kemudian dikonvert secara otomatis menjadi nested entity object.

Syarat menerapkan hal ini adalah, pada Dto dari suatu entitas itu harus menerima tipe data si Foreign Keynya. Kalau projek aku hanya menerima Long id aja.


10.16 Pagination
Pagination sendiri digunakan ketika kita ingin membatasi jumlah data yang ingin ditampilkan atau diambil supaya tidak memakan banyak resource ketika mengembalikanya, disitulah peran Pagination yang dimiliki oleh Spring data JPA.

Jadi caranyya kita implementasi suatu class menggunakan extends pada Object Repository (Persistance) yang awalnya punya CrudRepository<Entity, TipeId> aja, sekarang untuk Pagination kita tambahkan PagingAndSortingRepository dengan input parameter tipe yang sama.

Lalu pada Service Layer kita tambahkan metode yang ada pada class PagingAndSortingRepository yaitu findAll() dengan tipe data Page<Entitas> yang merupakan sebuah list dan menerima constructor (Pageable pageable) yang akan dimanfaatkan melalui DI.

Kemudian methode findAll() oleh Page tersebut cukup seperti mencari sebuah list yang berisikan semua isi data namun dengan input parameter Pageable pageable;

Lalu pada Presentation Layer atau Controller kita modifikasi si GetMapping List menjadi tipe Page<Dto> yang menerima parameter constructor Pageable. Sehingga kita mengembalikkan list Page entity yang diconver menjadi Dto

Sehingga cara memanfaatkannya di link api itu kita bisa GraphQL gitu:
GET: http://localhost:8080/kapal-kapal?size=20&page=5

Jadi dibatasi berapa data yang ditampilkan



===================================================================================
Project Task Tracker

1. Set up dev env
2. analysis project brief
3. design app
4. start and connect db
5. create app entity dan domain (relasi)
6. create Dtos
7. create mappers
8. create repositories jpa
9. implement rest api  & error handling
10. database transaction


Jadi disini kita bikin front end dan backend. Untuk segala kondisi MVP (Minimum Viable Product) yang artinya aplikasi kita yang penting mencapai tujuan.

Jadi aplikasi sederhana untuk mengurutkan daftar tugas paling penting. Satu daftar tugas, memiliki banyak tugass yang bisa diurutkan berdasarkan kepentingannya.

Jadi dari front end akan berkomunikasi dengan interface RestAPI terhadap backend. Lalu dari backend akan berkomunikasi melalui persistance layer menggunakan JPA terhadap database postgre.

Untuk api yang dibutuhkan yaitu:
1. GET		/task-lists
2. POST		/task-lists
3. GET		/task-lists/{task_list_id}
4. PUT		/task-lists/{task_list_id}
5. DELETE	/task-lists/{task_list_id}

6. GET		/task-lists/{task_list_id}/tasks
7. POST		/task-lists/{task_list_id}/tasks

8. GET		/task-lists/{task_list_id}/tasks/{task_id}
9. POST		/task-lists/{task_list_id}/tasks/{task_id}
10. DELETE	/task-lists/{task_list_id}/tasks/{task_id}

[40:00]


5. create app entity dan domain (relasi)

Kita membuat class Enum yang akan digunakan ketika suatu Entity memiliki variabel dengan tipe data bertingka seperti ordinal. 

Kita bikin dua entity yang setiap entitas mengandung instance variabel.

Kita bikin Domain atau relationship antar Entity. Jadi anggap aja kelas TugasDaftar memiliki list dari beberapa kelas Tugas. Sehingga relasinya adalah OneToMany dari kelas TugasDaftar. Pada kelas tersebut kita tambah anotasi @OneToMany dengan parameter khusus `mappedBy = "namavariabel"` berisi nilai nama variabel dari foreignkey di kelas Tugas.

Untuk menyambungkan foreign key, kelas Tugas harus menambah variabel dengan tipe kelas TugasDaftar dengan nama var yang serupa dengan nama mappedBy sebelumnya. Baru tambah anotasi @ManyToOne, dan hubungkan foreign key dengan anotasi @JoinColumn dengan parameter name = "" dengan isi nama variabel di tabel foreign key.


6. create Dtos
Kita bikin Dto nya untuk setiap tabel yang ingin di crud (termasuk read biasa). Dalam file bertipe Record yang bertujuan supaya data class bersifat immutable. Jadi file Record akan secara otomatis memiliki getter method (bukan setter, karena bersifat immutable).

Jadi kalau misalkan di Dto itu nggak harus berisi semua instance variabel pada Entity, cukup yang perlu kita gunakan di API aja. Maka dari itu sangat memungkinkan unuk variabel baru di Dto muncul dengan nilai dari hasil perhitungan suatu variabel di entitas.


7. create mappers
Implementasi Task Mapper
Disini logika gimana cara convert dari atau menuju Domain entities dengan Dto. Disini memungkinkan ada beberapa logika untuk mendapatkan nilai dari variabel di entitas kemudian dimasukkan ke variabel baru yang bisa diakses pada Dto. Walaupun logika kayak gini bisa juga di Service, tapi kita punya advantage dari logika ynag ada di mapper. yaitu:
- logika mapping terisolasi dan reusable
- transformasi konsisten pada aplikasi kita
- lebih rapih kodenya ketika di service dan controller



8. create repositories jpa
Implementasi Tugas Daftar Repository
Jadi tujuan dari Repository dengan anotasi @Repository adalah menyediakan fungsionalitas Crud, nah untuk Spring JPA sendiri sudah menyediakan secara otomatis yaitu exends JpaRepository<Entity, Id>. Kita bikin Repository (interface)


9. implement rest api  & error handling

GET List dari TugasDaftar
Kita bikiin Service Layer (interface) dan berisi fungsionalitas yang diharapkan, contohnya listGetTugasDaftar() yang mengambil setiap data dari Entity. Kemudian pada Controller (class) kita gunakan si method dari service layer tersebut kemudian kita convert jadi Dto.



Get one id of TugasDaftar
Cara nya sih sama kita bikin method getTugasDaftar berdasarkan id, namun disimpan dalam tipe Optional<TugasDaftar>

Update TugasDaftar
Caranya serupa sebagaimana tahapan untuk menambahkan service layer kemudian mempresentasikannya, nanti fungsi service layer disimpan dalam Entity dengan menerima UUID dan Entity. Pastikan validasi, lalu cari si Entity dengan method findById() melalui repository, lalu gunakan setNama, setDeskripsi.

Setelah itu panggil di controller dan ubah dulu dari Dto ke entity, and then return from entity into dto.

Delete Tugas Daftar:
Hapus satu TugasDaftar artinya menghapus semua Tugas yang ada.

Thats it Crud in TugasDaftar are done, next we move into Tugas that can be listed in TugasDaftar.



Crud functionality for Tugas:

Get daftar atau List dari Tugas dalam suatu TugasDaftar


Create Tugas:
Jadi caranya biasa okay dari Interface Service Layer then implement the method on Service Layer Impl, then implement the functionality on Controller.

Untuk bikin Tugas baru pada suatu TugasDaftar, artinya kita perlu tahu id dari TugasDaftar. Since membuat objek Tugas yang baru kita perlu membuat atau memanggil entity objek dari TugasDaftar melalui repository TugasDaftar.

Untuk variabel lainnya kita bisa menerapkan Optional terhadap variabel yang Enum atau bersifat ordinal, seperti prioritas dan status.


Get Tugas by Id, ini tujuannya untuk mengambil detail dari Tugas pada suatu TugasDaftar:
Untuk metode ini sebenarnya kita lebih baik menerapkan Query otomatis yang dieksekusi oleh Hibernate JPA secara otomatis. Jadi pada repository (yang mengextends Crud functionality) kita membuat metode Optional<Entity> dengan nama method:

findByTugasDaftarIdAndId(UUID tugasDaftarId, UUID id);

nanti hibernate akan mencari kombinasi query yang tepat dan come up with sebuah data dari suatu Tugas yang dimiliki oleh suatu TugasDaftar.



Update Tugas:
Caranya kita bikin method update, si method ini awalnya bakal ngambil Entity Tugas (dari id TugasDaftar dan id Tugas) menggunakan method query yang ada di repository yang mengambil Tugas dari TugasDaftar.

Kemudian seperti Put endpoint biasa, kita akan setVariabel lalu disimpan dalam repository.


Delete Tugas:
Disini kita menerapkan pendekatan baru dalam Delete suatu data yaitu menggunakan `transactions`. 

Pendekatan lama itu kita bisa hapus suatu TUgas berdasarkan method bawaan dari repository yaitu .deleteById, nah tapi method itu tidak memeriksa atau validasi si data yang mau dihapusnya.

Jadi penerapannya yaitu dengan menambah anotasi @Transactional di atas penerapan dari method deleteTugas. Kenapa? Karena deleteTugas berinteraksi dengan method deleteTugasDaftar yang secara default merupakan sebuah @Transactional juga dan selain itu ada beberapa calls ke arah Repository yang menjadi BERESIKO, maka dari itu method deleteTugas harus disamakan dengan menambah @Transactional.

Kenapa harus @Transactional di method delete? Anotasi Transactional memastikan proses delete itu terjadi dalam transaksi database, jadi ketika proses delete tersebut gagal, maka semua bagian aplikasi melakukkan rolled back.

Hal ini juga berlaku pada method lain pada TugasDaftar yang melakukan multiple call ke arah Repository, hal ini beresiko karena beberapa kali akses terbuka ke repository.



===================================================================================
Java Project with Spring Security:

1. Adding JWT authentication using Spring Security
2. Using Project Lombok
3. Using Mapstruct (Pengganti ObjekMapper?)


MapStruct adalah sebuah generator kode Java yang dirancang untuk mempermudah pembuatan pemetaan (mapping) antara objek Java bean. MapStruct adalah prosesor anotasi Java yang menghasilkan kode pemetaan antara kelas Java bean secara otomatis. Ini memungkinkan pengembang untuk mendefinisikan antarmuka pemetaan sederhana, dan MapStruct akan menghasilkan implementasi konkret pada saat kompilasi.

MapStruct sering digunakan dalam aplikasi multi-layer untuk memetakan objek antara berbagai lapisan, seperti:

- Memetakan entitas JPA ke DTO (Data Transfer Objects) dan sebaliknya.
- Mengkonversi objek antara lapisan persisten, lapisan service, dan lapisan presentasi.
- Menyederhanakan transformasi data dalam aplikasi Spring Boot.


Start Project:
1. Design Implementation (Plan about database diagram, logic, etc)
2. Create JPA Entities dan Dto (Domain)
3. Create JPA Repositories dan Services, sekaligus Mapper
4. Implement Category endpoints (Start Controller)
5. Add Spring Security
6. Configuring Security to use User
7. Adding a JWT authentication endpoint
8. Add JWT Auth filter
9. Implement Tag, then Post endpoints



1. Design Implementation
Lihat dua gambar domain dan tabel diagram tabel sebelumnya. Overall kita punya 5 Tabel. Pertama itu ada tabel User, user mampu memiliki beberapa Post sehingga relasinya one to many. Lalu ada tabel Post, tabel Post harus memiliki satu informasi mengenai tabel Kategori dengan relasi one to one. Tabel post juga bisa memiliki beberapa Tag atau Label sehingga relasinyna Many to Many. Terakhir, tabel Post memiliki relasi Many to Many terhadap tabel Enum Status (Draft atau Publish).

Jadi untuk tabel User, Post, Category, dan Tag memiliki id unik dengan tipe data UUID.

API:
Untuk api jika ada rute atau path yang menerapkan 'v1'. Artinya itu semacam versi dari pengembangan API, jadi mungkin next ada v2, etc.



Create User Entity (Pengguna):
Seperti biasa kita bikin entitas dari User dengan menggunakan fitur JPA Hibernate supaya membuat getter dan setter dan equal hashCode otomatis. Sebenarnya ini bisa otomatis dengan menggunakan @Data, namun karena kita menggunakan Mapstruct di entitas User kita harus bikin manual method equal( ) dan hashCode().

Hal unik:
Kita bisa tentuin nilai dari suatu instance variabel secara default jika mendapat null. Contohnya:
	@PrePersist
	protected void onCreate() {
		this.createAt = LocalDateTime.now();
	}

Method yang ada pada class Entitas tersebut menandakan bahwa variable createAt jika bernilai null, maka akan diset secara otomatis waktu yaitu sekarang.


[53:19] Create Post Entity


Kurang lebih sama, tapi jika ada instance variabel yang berasal dari Enum class, tolong panggil anotasi Enumerator dengan spesifik apa tipenya, karena biasanya kalau Enum itu dijadiin integer.


User Post relationship (one to many)


Dalam entitas Post, harus memiliki satu User, sehingga hubungannya Many to One:
	@ManyToOne(fetch = FetchType.LAZY)
	@JoinColumn(name = "author_id", nullable = false)
	private User author;


Kemudian dalam entitas yang menerima relasinya yaitu User,dengan relasi One to Many:

@OneToMany(mappedBy = "author", cascade = CascadeType.ALL, orphanRemoval = true)
private List<Post> posts = new ArrayList<>();

cascade ALL dan orphan removal tersebut artinya, memberikan akses untuk menghapus semua data pada foreign key Post jika suatu User dihapus.


Jika ada relasi Many to Many, contohnya di Post itu berupa Set (tidak boleh duplikat):

	@ManyToMany
	@JoinTable(name = "post_tags", joinColumns = @JoinColumn(name="post_id"), inverseJoinColumns = @JoinColumn(name="tag_id"))
	private Set<Tag> tags = new HashSet<>();

nama adalah nama variabel yang akan digenerate oleh Hibernate JPA, kemudian joinColumns itu asal dan inverseJoinColumns itu tujuan.



Persistance Layer untuk Crud functionality;
Create User Repository untuk autentikasi, manajemen User

Bikin Repository untuk Kategori

Kurang lebih untuk Repository kita pakai JpaRepository, karena dengan ini kita bisa akses HibernateJPA supaya secara otomatis bisa bikin fungsionalitas.


List Kategori Endpoint
Well cara bikin sebuah Controller sebenarnya dimulai dari membuat @Repository, setelah itu kita bisa bikin Service layer (interface dan class impl) untuk suatu entity. Service bertujuan untuk fungsionalitas dalam mengambil data dari Repository (persistance) dengan output berupa Entities.

Kemudian service layer dipanggil pada Controller (Presentation layer / @RestController) untuk menggunakan fungsionalitasnya dalam mengambil data dalam bentuk Entities. Tapi setiap fungsionalitas dalam controller harus dalam bentuk DTO (Data Transfer Object), maka dri itu kita harus membuat Mapper (dalam interface) untuk conver Entity  ><  DTO.

Untungnya ada library MapStruct yang mempermudah proses convert dari Entity ke Dto atau sebaliknya. Panggil anotasi @Mapper:
@Mapper(componentModel = "spring", unmappedTargetPolicy = ReportingPolicy.IGNORE)

Kemudian method fungsionalitas untuk convert harus ditandai dengan anotasi @Mapping:

@Mapping(target = "jumlahPost", source = "posts", qualifiedByName = "calculateJumlahPost")
KategoriDto toDto(Kategori kategori);

	@Named("calculateJumlahPost")
	default Long calculateJumlahPost(List<Post> posts) {
		if (posts == null) {
			return (long) 0;
		}
		
		return posts.stream()
				.filter(post -> PostStatus.PUBLISHED.equals(post.getStatus()))
				.count();
	}

Jadi pada method itu, ada variabel dalam Dto yang ingin numpang dalam mengambil informasi secara otomatis. Makannya pada parameter @Mapping kita masukkan nama methodnya untuk mengambil jumlah pada List Post yang merupakan relasional antara tabel Post. Makannnya method yang numpang harus dianotasi @Named untuk menandainya.

[1:26:23]

Create Kategori endpoint

Disini cukup unik cara membuat sebuah Dto kemudian disimpan dalam entity di database melalui Repository. Jadi tujuan kita itu ingin memvalidasi input yang dimasukkan dalam form atau string 'nama' pada tabel Kategori, caranya pada controller kita harus memasukkan parameter:
@Valid @RequestBody CreateKategoriRequest createKategoriRequest

Anotasi @Valid harus memastikan objek parameter harus dalam tipe tersebut. Objek CreateKategoriRequest fungsinya sama kayak KategoriDto namun dia hanya digunakan untuk satu variabel yaitu 'nama':

	@NotBlank(message = "Kategori tidak boleh kosong.")
	@Size(min = 2, max = 50, message = "Kategori harus memiliki minimal {min} dan maksimal {max} huruf.")
	@Pattern(regexp = "^[\\w\\s-]+$", message = "Kategori harus mengandung huruf, angka , spasi, dan hyphens")
	private String nama;


Intinya anotasi tersebut bertujuan untuk memvalidasi isi dari 'name' yang akan diinput pada presentation layer (controller). Kemudian CreateKategoriRequest harus diconvert menjadi Entity di Mapper.


Delete Category Endpoint:


Add Spring Security;

Jadi cara menambahkan Spring Security yang salah satunya:

1.**memberikan akses JWT authentication pada suatu endpoint**. 

Maka dari itu kita setting pom.xml kemudian bikin folder config untuk file SecurityConfig dengan anotasi @Congiguration.

Dalam file tersebut kita membuat @Bean method yang return objek SecurityFilterChain dengan input parameter HttpSecurity. Dari parameter tersebut kita akan memberi akses JWT ke beberapa endpoint seperti di bawah ini:

	@Bean
	public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception{
		http
				.authorizeHttpRequests(auth -> auth
						.requestMatchers(HttpMethod.GET, "/api/v1/posts/**").permitAll()
						.requestMatchers(HttpMethod.GET, "/api/v1/categories/**").permitAll()
						.requestMatchers(HttpMethod.GET, "/api/v1/tags/**").permitAll()
						.anyRequest().authenticated()
						)
				.csrf(csrf -> csrf.disable())
				.sessionManagement(session -> session.sessionCreationPolicy(SessionCreationPolicy.STATELESS));
		
		return http.build();
	}

Selain dari http yang diauthorize permit all, maka itu memerlukan autentikasi.

2. **Melakukan encode(merubah) terhadap password**:
Caranya dengan membuat @Bean yang return PasswordEncoder melalui class PasswordEncoderFactories dengan method createDelegatingPasswordEncoder().

3. Melakukan Autentikasi:
Caranya dengan membuat Bean yang return AuthenticationManager dengan input parameter AuthenticationConfiguration config, lalu melakukan return config.getAuthenticationManager();.

Cara mereka saling berkomunikasi itu dikarenakan anotasi @Configuration dan @Bean ok

Jadi dengan mengkonfigurasi 3 hal tersebut menggunakan Spring Security kita bisa membatasi beberapa endpoint yang mana yang bisa diakses tanpa authentication.

[1:53:06]


Loading Users:
Functionality untuk mengaktifkan autentikasi berdasarkan entiti user yang ada. Implementasi ini akan menjembatani komunikasi antara spring security user managameent dan modul domain.

Cara untuk memberikan beberapa fungsionalitas mengenai akses User dalam autentikasi kita bisa menerapkan interface UserDetails (dari Spring security) terhadap class "BarangUserDetails", lalu disini kita override setiap method. Untuk method `Collection<? extends GrantedAuthority> getAuthorities()` kita berikan role USER. Dan method lainnya seperti getPassword, getUsername (email), dll.

Pertanyaannya adalah bagaimana cara kita mengetahui si User melalui email? Caranya adalah dengan menambahkan method Hibernate Jpa pada Repository untuk mengembalikan Optional<User> dengan method findByEmail dengan input String email. Sehingga kita punya method untuk mencari data User berdasarkan email.

Setelah membuat method untuk mencari User entity berdasarkan email. Kita memanfaatkannya dengan menerapkan interface UserDetailsService melalui method `UserDetails loadUserByUsername(String email)` untuk melakukan load User. Ketika entitas User sudah ditemukan berdasarkan email, kita return menjadi UserDetails menggunakan class sebelumnya yang berperan dalam fungsionalitas autentikasi yaitu "BarangUserDetails".


Authenticate Endpoint Part 1:
Endpoint yang bertujuan untuk mengobtain JWT's, endpoint ini bakal validate user credential dan return JWT yang akan digunakan oleh client di beberapa request endpoint api.


Proses Autentikasi supaya bisa benar-benar berhasil memerlukan beberapa Dto untuk handle login request dan response. Disini kita bikin Dto untuk LoginRequest yang menampung instance variabel email dan passworrd, lalu Dto AuthResponse yang mengandung token dan expiresIn (Long waktu).

Setelah ada Dto, selanjutnya kita buat fungsionalitas proses autentikasi ke database (persistance) dengan membuat interface Service untuk Authentication. Interface tersebut berisi method untuk mengambil UserDetails (yang sudah divalidasi melalui UserRepository) dan method untuk mengembalikan token dengan input parameter UserDetails. 

*UserDetails adalah class bawaan dari Spring Security, yang sudah kita konfigurasi beberapa fungsionalitas supaya dapat mencari entitas User dari database kita.

[2:06:56]


Authenticate Endpoint Part 2:
Di sini kita menerapkan interface dari Service autentikasi pada class Impl kita. Kita mendefinisikan kunci jwt secret 	

# Penjelasan Konsep Autentikasi JWT dalam Kode

Kode yang Anda tunjukkan merupakan implementasi layanan autentikasi menggunakan JWT (JSON Web Token) dalam aplikasi Spring Boot. Mari saya jelaskan konsep-konsep utamanya:

## Proses Autentikasi

1. **Autentikasi Pengguna**: 
   - Metode `authenticate()` menerima email dan password
   - Menggunakan `AuthenticationManager` untuk memverifikasi kredensial tersebut
   - Jika berhasil, data pengguna diambil melalui `UserDetailsService`

2. **Pembuatan Token JWT**:
   - Setelah autentikasi berhasil, `generateToken()` membuat token akses
   - Token ini berisi informasi penting tentang pengguna dan masa berlakunya

## Mengapa Menggunakan JWT Secret Key

JWT secret key sangat penting karena beberapa alasan:

1. **Keamanan Token**: 
   - Secret key digunakan untuk "menandatangani" token
   - Ini memastikan bahwa token hanya dapat dibuat atau diverifikasi oleh aplikasi yang memiliki kunci tersebut

2. **Integritas Data**:
   - Mencegah modifikasi token oleh pihak luar
   - Jika token diubah, verifikasi tanda tangan akan gagal

3. **Kepercayaan**:
   - Server dapat memastikan bahwa token yang diterima benar-benar dikeluarkan oleh sistem yang sama

## Konsep Security Key

Kode menggunakan `Keys.hmacShaKeyFor()` untuk menghasilkan kunci keamanan karena:

1. **Standar Industri**:
   - HMAC-SHA256 adalah algoritma kriptografi standar yang aman
   - Memberikan keseimbangan antara keamanan dan kinerja

2. **Stateless Authentication**:
   - Memungkinkan server memverifikasi pengguna tanpa menyimpan sesi
   - Server cukup memverifikasi tanda tangan token untuk memvalidasi pengguna

## Struktur Token JWT

Token yang dihasilkan berisi:

1. **Claims** (klaim) - informasi tentang subjek token
2. **Subject** - identitas pengguna (username/email)
3. **Issued At** - waktu token dibuat
4. **Expiration** - waktu kedaluwarsa token (dikonfigurasi 24 jam atau 86400000 milidetik)

## Keuntungan Pendekatan Ini

1. **Skalabilitas**: Server tidak perlu menyimpan informasi sesi
2. **Keamanan**: Token ditandatangani secara kriptografis
3. **Efisiensi**: Verifikasi cepat tanpa perlu query database
4. **Fleksibilitas**: Token dapat digunakan di berbagai layanan (microservices)

Secara keseluruhan, implementasi ini mengikuti praktik terbaik untuk autentikasi berbasis token di aplikasi modern, dengan penekanan pada keamanan dan efisiensi.



JWT Authentication filter part 1:
Disini kita letak filenya di package security. Jadi JwtAuthenticationFilter adalah komponen kunci dalam arsitektur keamanan Spring yang:

1. Memproses Setiap Request:
- Extends OncePerRequestFilter sehingga dijalankan tepat satu kali untuk setiap permintaan HTTP
- Ini penting untuk konsistensi dalam autentikasi

2. Posisi dalam Filter Chain:
- Filter ini berada dalam rantai filter Spring Security
- Dijalankan sebelum request mencapai controller atau endpoint lainnya


Proses Validasi Token
Saat permintaan masuk, filter melakukan beberapa langkah penting:

1. Ekstraksi Token:
- Memeriksa header HTTP Authorization untuk pola "Bearer [token]"
- Mengekstrak token JWT dengan menghapus prefix "Bearer "

2. Validasi dan Autentikasi:
- Memanggil authenticationService.validateToken() untuk memverifikasi token
- Jika valid, mendapatkan objek UserDetails yang berisi informasi pengguna

3. Keamanan Konteks:
- Membuat objek autentikasi UsernamePasswordAuthenticationToken
- Menyimpannya di SecurityContextHolder sehingga seluruh aplikasi dapat mengakses informasi pengguna yang terautentikasi


Penanganan User Context
Filter ini juga memperkaya request dengan informasi tambahan:

- Jika user details adalah instance dari BarangUserDetails, ID pengguna ditambahkan ke request attributes (awalnya hanya email sebagai username)
- Ini memungkinkan controller atau layer lain mengakses ID pengguna tanpa parsing token lagi


JWT Authentication filter part 2:
# Penjelasan Konfigurasi Keamanan Spring Security

Kode yang Anda tunjukkan adalah konfigurasi keamanan Spring Security untuk aplikasi Anda. Dibandingkan dengan kode sebelumnya, ini menunjukkan kerangka lengkap bagaimana JWT filter diintegrasikan ke dalam aplikasi.

## Perbedaan Utama dan Konteksnya

Kode ini berbeda dari sebelumnya karena:

1. **Konfigurasi Lengkap Security**: 
   - Ini adalah kelas konfigurasi (`@Configuration`) yang mendefinisikan seluruh setup keamanan aplikasi
   - Kode sebelumnya hanya menunjukkan implementasi filter JWT dan layanan autentikasi

2. **Dependency Injection**: 
   - Kode ini mendeklarasikan semua komponen keamanan sebagai bean Spring
   - Menunjukkan bagaimana komponen-komponen security saling terhubung

3. **Filter Chain Configuration**:
   - Menentukan aturan lengkap untuk endpoint mana yang memerlukan autentikasi
   - Mengkonfigurasi bagaimana filter JWT diintegrasikan ke dalam pipeline pemrosesan request

## Komponen Utama dan Konteksnya

1. **UserDetailsService Bean**:
   - Membuat layanan yang bertanggung jawab untuk memuat data pengguna
   - Membuat pengguna test otomatis jika tidak ada di database
   - Memungkinkan aplikasi mengautentikasi pengguna berdasarkan data dari repository

2. **SecurityFilterChain**:
   - Mendefinisikan aturan autentikasi dan otorisasi
   - Mengizinkan akses publik ke endpoint login dan beberapa endpoint GET
   - Mengharuskan autentikasi untuk semua request lainnya
   - Menonaktifkan CSRF untuk API stateless
   - Mengkonfigurasi manajemen sesi sebagai STATELESS (menghindari penggunaan session cookies)

3. **PasswordEncoder**:
   - Mendefinisikan encoder untuk mengamankan password
   - Menggunakan DelegatingPasswordEncoder yang mendukung berbagai algoritma encoding

4. **AuthenticationManager**:
   - Komponen inti yang menangani proses autentikasi
   - Digunakan oleh AuthenticationService untuk memverifikasi kredensial pengguna

5. **JwtAuthenticationFilter**:
   - Menghubungkan filter JWT yang kita lihat sebelumnya ke dalam pipeline keamanan
   - Mendefinisikan urutan filter (dijalankan sebelum UsernamePasswordAuthenticationToken)

## Konteks Keseluruhan

Kode ini menunjukkan implementasi lengkap sistem autentikasi berbasis token:

1. **API Stateless**: 
   - Aplikasi didesain untuk bekerja tanpa state server (tanpa session)
   - Setiap request diautentikasi secara individual menggunakan token JWT

2. **Keamanan Berlapis**:
   - Kombinasi antara password encryption, JWT signed tokens, dan filter autentikasi
   - Endpoint publik ditentukan secara eksplisit, semua endpoint lainnya terlindungi

3. **Testing Support**:
   - Pembuatan pengguna test otomatis memudahkan pengembangan dan pengujian
   - Memastikan ada setidaknya satu pengguna yang dapat digunakan untuk testing

Secara keseluruhan, konfigurasi ini menghubungkan semua komponen keamanan yang telah kita bahas sebelumnya ke dalam satu arsitektur keamanan yang komprehensif dan modern untuk aplikasi Spring Boot.

2:31:07

-----------------------------------------------------------------------------------
Proses JWT Autentikasi secara keseluruhan:

Dalam pengembangan web modern, autentikasi pengguna adalah komponen penting untuk memastikan keamanan aplikasi. Dua metode yang sering digunakan adalah Session dan JSON Web Token (JWT). Meskipun tujuan keduanya sama—yaitu mengidentifikasi dan mengautentikasi pengguna—mereka bekerja secara berbeda dan memiliki kelebihan serta tantangan masing-masing. Video ByteByteGo ini mengupas secara mendalam mengenai perbedaan yang mungkin tidak Anda ketahui di antara kedua pendekatan tersebut.

2. Apa itu Session?
Session merupakan mekanisme berbasis server untuk menyimpan data autentikasi pengguna. Berikut adalah beberapa poin utama:

- Penyimpanan Server-side: Ketika pengguna berhasil login, server membuat session yang menyimpan data penting (seperti ID pengguna, peran, dan informasi lainnya) di sisi server (bisa dalam memori, database, atau store seperti Redis).
- Session ID di Cookie: Server mengirimkan session ID ke klien melalui cookie. Pada setiap permintaan berikutnya, browser mengirimkan cookie ini agar server dapat mengenali session yang telah dibuat.
- Stateful: Karena data autentikasi disimpan di server, sistem menjadi stateful. Artinya, server harus memelihara dan mengelola setiap session pengguna.
- Kemudahan Revoke: Karena data disimpan di server, server dapat dengan cepat menghapus atau memvalidasi ulang session—misalnya saat logout atau jika terjadi kompromi keamanan.


3. Apa itu JWT?
JSON Web Token (JWT) adalah standar token yang bersifat self-contained dan stateless. Beberapa karakteristiknya antara lain:

- Token Mandiri (Self-contained): JWT menyimpan semua klaim (data) yang dibutuhkan di dalam token itu sendiri, yang kemudian dikodekan dan ditandatangani secara digital. Ini memungkinkan server untuk memverifikasi keabsahan token tanpa menyimpan data sesi secara terpusat.
- Penyimpanan Client-side: JWT biasanya disimpan di localStorage, sessionStorage, atau sebagai cookie. Karena token ini berisi informasi penting, keamanan penyimpanannya perlu diperhatikan.
- Stateless: Server tidak perlu menyimpan data autentikasi karena semua informasi ada dalam token. Hal ini memberikan keuntungan skalabilitas terutama pada arsitektur microservices atau sistem terdistribusi.
- Tantangan Revocation: Karena JWT bersifat stateless, mencabut akses (misalnya saat logout) tidak bisa dilakukan secara instan kecuali jika diterapkan mekanisme tambahan seperti blacklist atau pengaturan waktu kedaluwarsa yang pendek.




-----------------------------------------------------------------------------------


List Tag Endpoint

Create Tag Endpoint


Uniknya untuk kedua fungsionalitas tersebut kita punya Dto nya masing-masing. Untuk List all Tag kita pakai TagResponse, dan CreateTagRequest

Jika ada Dto yang namanya Request artinya kita mengirim Dto ke server., disini hanya yang diperlukan input field saja. Jika id tidak perlu ok. karena Dto request hanya digunakan pada @RequestBody. Namun tetap dari service methodnya hanya menerima nama tag baru saja.

Delete Tag Endpoint

Lagi-lagi karena Tag merupakan sebuah set dan memiliki instance variabel dari Posts. Sehingga kita harus memanggil repository dari tag dan memeriksa apakah getPost kosong atua tidak, jika tidak kita throw exception, jika memang kosong maka kita bisa hapus tag menggunakan method deleteById;


List All Post Endpoint:
Di sini caranya seperti biasa, namun ketika service implementaion kita menggunakan method yang dibantu oleh hibernate JPA dalam mengambil data. Karena post itu bisa beberapa versi seperti list post jika menampilkan kategori saja, atau tag saja, atau keduanya, atau semuanya. SEhingga ktia bikin beberapa method hibernate jpa di Repository post.

Lalu pada Mapper post, jangan lupa kaitkan bahwa method convert toDto itu juga @Mapping melalui tabel kategori, tag, dan author.

Author adalah dto baru sebagai sinonim dari User, supaya data user tidak tersebar,


List Draft Posts Endpoint:
Caranya sama namun controllernya menerima input id dari user yang logged in. Lalu untuk penerapannya di impl service kita bisa menggunakan method hibernate hql dalam post repository untuk mengambill list dari posts berdasarkan User dan PostStatus.


Create Post Endpoints;
Disini memang ada beberapa kriteria supaya ketika post suatu Post (artikel) maka mengambil informasi foreign key dari beberapa table.

1. Karena entity Post memerlukan objek seperti Kategori, User, dll. Kita perlu membuat Dto sebagai @RequestBody yang hanya menerima UUID saja. Dto ini hanya digunakan untuk komunikasi dengan presentation layer aja. Pada class ini pastikan ada batasan untuk validasi, seperti maksimal 50 kata, dll
2. Lalu untuk Dto yang berkomunikasi dengan service layer, kita bikin Dto baru dengan tipe dan instance variabel yang serupa.
3. Untuk beberapa Service pada kelas seperti User, Tag, dan Kategori usahakan ada method yang mengambil entity objek melalui uuid yang disimpan oleh Dto post service layer. 
4. Pada implementasi dari Service layer Post, disini kita membuat method createPost yang return entity Post dengan parameter request Post (dto untuk service layer) dan entity User.


Update post endpoints

Kurang lebih hampir sama metodenya dengan ketika membuat Post Endpoint, kita bikin Dto baru untuk presentatin dan service layer. Then ambil dulu Post yang ingin diupdate berdasarkan UUid


Get Post Endpoint
Untuk mengambil a single informasi Entity melalui sebuah Dto. Kurang lebih sama intinya kita menggunakan method findById dengn input parameter UUID.


Delete Post Endpoint



====================================================================================
